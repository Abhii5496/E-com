import React from "react";
import { Link } from "react-router-dom";

const Profile =() => {
    return (
        <div>
            <h1 className="profile"> Working on it...........</h1>
        </div>
    )
    }

    export default Profile;